{%- comment -%} Auto Generated Variables start {%- endcomment -%}
{%- liquid
  assign block_type = "option__color"
  assign section_type = "card_product-card"
  assign legend = block.settings.legend
  assign legend_class = block.settings.legend_class
  assign primary_source = block.settings.primary_source
  assign fallback_source = block.settings.fallback_source
  assign size_border_width_radius = block.settings.size_border_width_radius
  assign hover_border_color = block.settings.hover_border_color
  assign outline_width_offset = block.settings.outline_width_offset
  assign hover_outline_color = block.settings.hover_outline_color
  assign show_tooltip = block.settings.show_tooltip
  assign margin_top_gap = block.settings.margin_top_gap
  assign content_flow = block.settings.content_flow
  assign match_option_titles = block.settings.match_option_titles
  assign match_exact_word = block.settings.match_exact_word
  assign margin_top_bottom_left_right = block.settings.margin_top_bottom_left_right
  assign max_w_flex_col_row_order_align = block.settings.max_w_flex_col_row_order_align
  assign responsive_visibility = block.settings.responsive_visibility
  assign hide_if_empty = block.settings.hide_if_empty
  assign responsive_breakpoint = block.settings.responsive_breakpoint
  assign anchor_id = block.settings.anchor_id
  assign show_conditionally = block.settings.show_conditionally
-%}
{%- comment -%} Auto Generated Variables end {%- endcomment -%}

{% liquid
  case content_flow
    when "show_all"
      assign container_classes = 'flex-wrap overflow-hidden'
    when "truncate"
      assign container_classes = 'overflow-hidden'
    when "overflow"
      assign container_classes = 'overflow-x-auto'
  endcase

  assign outline_classes = "!outline-none border-none ac:border-solid"
  assign outline_width = outline_width_offset | strip_newlines | split: '|' | first
  assign outline_width_desktop = outline_width | split: '/' | first
  assign outline_width_mobile = outline_width | split: '/' | last
%}

    
  <style>
    [data-style-id="{{ section.id }}--{{ block.id }}"] [data-style-label].active {
      border-color: {{ hover_border_color }};
    }
    [data-style-id="{{ section.id }}--{{ block.id }}"] [data-style-label].active:after {
      inset: 0;
      border-width: {{ outline_width_desktop | rstrip | replace: 'px', '' | append: 'px' }};
      border-color: {{ hover_outline_color }};
      border-radius: inherit;
      position: absolute;
      content: '';
    }
    @media screen and (max-width: {{ responsive_breakpoint | default: "768px" }}) {
      [data-style-id="{{ section.id }}--{{ block.id }}"] [data-style-label].active:after {
        border-width: {{ outline_width_mobile | rstrip | replace: 'px', '' | append: 'px' }};
      }
    }

  </style>

{% render '_utils.dynamic-css',
    block: block,
    selector: "& [data-style-container]",
    keys: "margin_top_gap" %}

{% render '_utils.dynamic-css',
    block: block,
    selector: "& [data-style-label]",
    keys: "size_border_width_radius, hover_outline_color" %}

{% render '_utils.dynamic-css', block: block, keys: "margin_top_bottom_left_right,max_w_flex_col_row_order_align,responsive_visibility" %}

<div
    data-style-id="{{ section.id }}--{{ block.id }}"
    {% if anchor_id != blank %}id="{{ anchor_id }}"{% endif %}
    {{ block.shopify_attributes }}
    x-show="$data.showConditionally('{{ show_conditionally }}') && !!card?.product?.options_with_values?.[getOptionIndex(`{{ block.id }}`)]">
  <fieldset x-data="{ hoverValue: '', index: getOptionIndex(`{{ block.id }}`), primary_source: `{{ primary_source }}`, fallback_source: `{{ fallback_source }}` }"
            x-effect="() => { if (card?.product?.id) { index = getOptionIndex(`{{ block.id }}`) } }"
            x-show="index !== -1 && card?.hydrated && card?.product?.variants?.length && card?.product?.variants?.[0]?.title !== 'Default Title' && card?.selected_variant?.id"
            x-cloak
            data-style-id="{{ section.id }}--{{ block.id }}">
    <legend class="flex w-full max-w-full truncate {{ legend_class }}"
            data-legend="{% render '_input.inline_richtext', content: legend, escape: true, block: block, forloop: forloop %}"
            x-effect="$el.innerHTML = $el.getAttribute('data-legend').replace('[heading]',card?.product?.options?.[index]).replace('[value]', hoverValue || card?.options?.[index])"
            style="--mb-md: {{ heading_space | first }};--mb: {{ heading_space | last }};">
    </legend>
    <div class="relative w-full flex scrollbar-none {{ container_classes }}"
         data-style-container
         x-data="{ offset: card?.product?.options_with_values?.[index]?.values?.length }">
      <template x-for="value in card?.product?.options_with_values?.[index]?.values?.slice(0, offset) ?? []">
        <label data-loop-item
               data-style-label
               class="flex shrink-0 cursor-pointer border-gray-300 relative {{ outline_classes }}"
               x-effect="$el.classList.toggle('active', card?.options[index] === value)"
               :class="hasAvailableVariant(index, value) ? '' : 'opacity-40'"
            {% if show_tooltip %}
              @mouseenter="$tooltip.addTooltip($el, value)"
              @mouseleave="$tooltip.removeTooltip($el)"
            {% endif %}
               @pointerenter="hoverValue = value"
               @pointerleave="hoverValue = ''"
               :aria-label="value"
               :style="getOptionSwatches(primary_source, fallback_source, value, index)">
          <input type="radio"
                 class="sr-only"
                 :name="`${card?.random_id}-${card?.product?.id}-${card?.product?.options[index]}-option__color`"
              {% if show_tooltip %}
                @focus="$tooltip.addTooltip($el.parentElement, value)"
                @blur="$tooltip.removeTooltip($el.parentElement)"
              {% endif %}
                 @change="$data.setProductOption({ index, value })"
                 :checked="card?.options[index] === value"
                 :value="value">
        </label>
      </template>
      {% if content_flow == 'truncate' %}
        <div class="flex shrink-0 items-center text-sm font-medium"
             :class="offset < card?.product?.options_with_values?.[index]?.values?.length ? '':'opacity-0 pointer-events-none'"
             x-init="$nextTick(async ()=> { if (window.design_mode) { await utils.delay(20) } offset = truncateChildren($el, $el.parentElement)})"
             @resize.window.debounce.200ms="offset = card?.product?.options_with_values?.[index]?.values?.length; await utils.delay(1); offset = truncateChildren($el, $el.parentElement)"
             x-html="`+ ${card?.product?.options_with_values[index]?.values?.length - offset} ${card?.product?.options[index]}`">
          +10 placeholder
        </div>
      {% endif %}
    </div>
  </fieldset>
</div>
