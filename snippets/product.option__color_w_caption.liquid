{%- comment -%} Auto Generated Variables start {%- endcomment -%}
{%- liquid
  assign block_type = "option__color_w_caption"
  assign section_type = "product"
  assign legend = block.settings.legend
  assign legend_class = block.settings.legend_class
  assign primary_source = block.settings.primary_source
  assign fallback_source = block.settings.fallback_source
  assign size_border_width_radius = block.settings.size_border_width_radius
  assign outline_width_offset = block.settings.outline_width_offset
  assign hover_outline_color = block.settings.hover_outline_color
  assign show_tooltip = block.settings.show_tooltip
  assign caption = block.settings.caption
  assign caption_class = block.settings.caption_class
  assign flex_direction_align_x_align_y_gap_wrap = block.settings.flex_direction_align_x_align_y_gap_wrap
  assign padding_top_bottom_left_right = block.settings.padding_top_bottom_left_right
  assign border_width_radius = block.settings.border_width_radius
  assign text_color = block.settings.text_color
  assign background_color = block.settings.background_color
  assign border_color = block.settings.border_color
  assign margin_top_gap = block.settings.margin_top_gap
  assign content_flow = block.settings.content_flow
  assign match_option_titles = block.settings.match_option_titles
  assign match_exact_word = block.settings.match_exact_word
  assign margin_top_bottom_left_right = block.settings.margin_top_bottom_left_right
  assign max_w_flex_col_row_order_align = block.settings.max_w_flex_col_row_order_align
  assign responsive_visibility = block.settings.responsive_visibility
  assign hide_if_empty = block.settings.hide_if_empty
  assign responsive_breakpoint = block.settings.responsive_breakpoint
  assign anchor_id = block.settings.anchor_id
  assign show_conditionally = block.settings.show_conditionally
-%}
{%- comment -%} Auto Generated Variables end {%- endcomment -%}

{% liquid
  assign option = product.options_with_values[optionIndex]

  if request.design_mode != true
    if option == blank
      assign is_empty = true
    endif
    if hide_if_empty == 'block' and is_empty
      echo '<script type="block/hide"></script>'
      break
    endif
    if hide_if_empty == 'container' and is_empty
      echo '<script type="container/break"></script>'
      break
    endif
    if hide_if_empty == 'section' and is_empty
      echo '<script type="section/break"></script>'
      break
    endif
  endif

  if option == blank
    break
  endif


  assign current_value = product.selected_or_first_available_variant.options[optionIndex]
  assign content_legend = legend | replace: "[heading]", option.name
  assign current_legend = content_legend | replace: "[value]", current_value | default: option.name

  if product.variants.first.title == "Default Title" and product.variants.size == 1
    break
  endif

  case content_flow
    when "show_all"
      assign container_classes = 'flex-wrap'
    when "truncate"
      assign container_classes = 'overflow-hidden'
    when "overflow"
      assign container_classes = 'overflow-x-auto'
  endcase
%}

{% render '_utils.dynamic-css',
    block: block,
    selector: "& [data-style-option-color-w-caption-container]",
    keys: "margin_top_gap" %}

{% render '_utils.dynamic-css',
    block: block,
    selector: "& [data-style-label-image]",
    keys: "size_border_width_radius, outline_width_offset, hover_outline_color" %}

{% render '_utils.dynamic-css',
    block: block,
    selector: "& [data-style-label]",
    keys: "flex_direction_align_x_align_y_gap_wrap, padding_top_bottom_left_right,border_width_radius,text_color,background_color,border_color" %}

{% render '_utils.dynamic-css',
    block: block,
    keys: "responsive_visibility,max_w_flex_col_row_order_align,margin_top_bottom_left_right" %}

<div
    data-style-id="{{ section.id }}--{{ block.id }}"
    {% if anchor_id != blank %}id="{{ anchor_id }}"{% endif %}
    {{ block.shopify_attributes }}
    x-show="$data.showConditionally('{{ show_conditionally }}')"
    data-block-type="{{ block.type }}">
  <fieldset x-data="{ index: +$el.getAttribute('data-index'), hoverValue: '' }"
            data-index="{{ optionIndex }}">
    <legend class="flex w-full max-w-full truncate {{ legend_class }}"
            data-legend="{% render '_input.inline_richtext', content: content_legend, escape: true, block: block, forloop: forloop %}"
            x-effect="$el.innerHTML = $el.getAttribute('data-legend').replace('[value]', hoverValue || $data.state.options[index])">
      {% render '_input.inline_richtext', content: current_legend, block: block, forloop: forloop %}
    </legend>
    <div class="relative w-full flex items-start scrollbar-none {{ container_classes }}"
         x-data="{ offset: +`{{ option.values.size }}` }"
         data-style-option-color-w-caption-container>
      {% for value in option.values %}

        {% liquid
          assign optionIndex1 = optionIndex | plus: 1
          assign option_num = 'option' | append: optionIndex1
          assign first_option_variant = product.variants | where: option_num, value | first
          assign _value = value | handleize

          case primary_source
            when "title"
              assign primary_swatch = "--primary-swatch: var(--swatch-" | append: _value | append: ")"
            when "image"
              assign image = first_option_variant.image | image_url: width: 60, height: 60, crop: 'center' | escape
              assign primary_swatch = "--primary-swatch: url(" | append: image | append: ") center center/cover no-repeat"
            when "metafield"
              if first_option_variant.metafields.smart.color_swatch.type == 'color'
                assign primary_swatch = "--primary-swatch: " | append: first_option_variant.metafields.smart.color_swatch.value
              endif
              if first_option_variant.metafields.smart.color_swatch.type == 'file_reference'
                assign image = first_option_variant.metafields.smart.color_swatch.value | image_url: width: 60, height: 60, crop: 'center' | escape
                assign primary_swatch = "--primary-swatch: url(" | append: image | append: ") center center/cover no-repeat"
              endif
          endcase
          case fallback_source
            when "title"
              assign fallback_swatch = "--fallback-swatch: var(--swatch-" | append: _value | append: ")"
            when "image"
              assign image = first_option_variant.image | image_url: width: 60, height: 60, crop: 'center' | escape
              assign fallback_swatch = "--fallback-swatch: url(" | append: image | append: ") center center/cover no-repeat"
            when "metafield"
              if first_option_variant.metafields.smart.color_swatch.type == 'color'
                assign fallback_swatch = "--fallback-swatch: " | append: first_option_variant.metafields.smart.color_swatch.value
              endif
              if first_option_variant.metafields.smart.color_swatch.type == 'file_reference'
                assign image = first_option_variant.metafields.smart.color_swatch.value | image_url: width: 60, height: 60, crop: 'center' | escape
                assign fallback_swatch = "--fallback-swatch: url(" | append: image | append: ") center center/cover no-repeat"
              endif
          endcase
        %}


        <label class="shrink-0 flex !outline-none cursor-pointer group {% if value == current_value %}active{% endif %}"
               data-style-label
               x-effect="$el.classList.toggle('active', $data.state.options[index] === $el.getAttribute('aria-label'))"
               x-show="offset >= +`{{ forloop.index0 }}`"
            {% comment %} TODO: Get a proper fix in {% endcomment %}
               :class="$data.hasAvailableVariant(index, `{{ value }}`) ? '' : ''"
               aria-label="{{ value }}">
          <input type="radio"
                 class="sr-only"
              {% if show_tooltip %}
                @focus="$tooltip.addTooltip($el.parentElement, $el.value)"
                @blur="$tooltip.removeTooltip($el.parentElement)"
              {% endif %}
                 name="{{ block.id }}"
                 @change="$data.setProductOption({ index, value: $el.value })"
                 :checked="$data.state.options[index] === $el.value"
                 value="{{ value }}">
          <picture
              class="group-ac:outline border-gray-300 shrink-0"
              data-style-label-image
              {% if show_tooltip %}
                @mouseenter="$tooltip.addTooltip($el, $el.getAttribute('aria-label'))"
                @mouseleave="$tooltip.removeTooltip($el)"
              {% endif %}
              aria-label="{{ value }}"
              @pointerenter="hoverValue = $el.getAttribute('aria-label')"
              @pointerleave="hoverValue = ''"
              style="{{ primary_swatch }};{{ fallback_swatch }};background: var(--primary-swatch, var(--fallback-swatch, {{ _value }}))"></picture>
          <div class="{{ caption_class }}">
            {% assign _caption = caption | replace: '[value]', value %}
            {% render '_input.inline_richtext', content: _caption, block: block, forloop: forloop %}
          </div>
        </label>
      {% endfor %}
      {% if content_flow == 'truncate' %}
        <div class="flex shrink-0 items-center text-sm font-medium"
             :class="offset < +`{{ option.values.size }}` ? '':'opacity-0 pointer-events-none'"
             x-init="$nextTick(async ()=> { if (window.design_mode) { await utils.delay(20) } offset = truncateChildren($el, $el.parentElement)})"
             @resize.window.debounce.200ms="offset = +`{{ option.values.size }}`; await utils.delay(20); offset = truncateChildren($el, $el.parentElement)"
             x-html="`+ ${+`{{ option.values.size }}` - (offset + 1)} {{ option.name }}`">
          +10 {{ option.name }}
        </div>
      {% endif %}
    </div>
  </fieldset>
</div>
